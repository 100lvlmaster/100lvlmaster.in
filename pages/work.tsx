import type { NextPage } from "next";
import MainLayout from "../layouts/main-layout";

const WorkPage: NextPage = () => {
  return (
    <MainLayout>
      <div>Adding stuff in progress...</div>
    </MainLayout>
  );
};
export default WorkPage;
